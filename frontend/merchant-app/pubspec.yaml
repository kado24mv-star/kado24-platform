name: kado24_merchant_app
description: Kado24 Cambodia - Merchant Mobile App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  provider: ^6.1.1
  
  # Networking
  http: ^1.1.0
  
  # Local Storage
  shared_preferences: ^2.2.2
  
  # QR Code Scanner
  qr_code_scanner: ^1.0.1
  mobile_scanner: ^3.5.5
  
  # UI Components
  fl_chart: ^0.65.0
  
  # Utilities
  intl: ^0.18.1
  
  # File Picker
  file_picker: ^6.1.1
  
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

































