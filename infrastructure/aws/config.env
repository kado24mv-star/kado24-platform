# AWS Configuration
# This file contains your AWS account configuration
# Source this file before running deployment scripts: source config.env

export AWS_ACCOUNT_ID=577004485374
export AWS_REGION=us-east-1
export ECS_CLUSTER_NAME=kado24-cluster
export PROJECT_NAME=kado24

# ECR Base URL
export ECR_BASE="${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com"

# Terraform Backend (optional - uncomment if using S3 backend)
# export TF_BACKEND_BUCKET="kado24-terraform-state-${AWS_ACCOUNT_ID}"

